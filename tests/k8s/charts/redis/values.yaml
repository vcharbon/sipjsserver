image:
  repository: redis
  tag: "7-alpine"
  pullPolicy: IfNotPresent

# Single replica — Phase A explicitly does not test dual-write.
# Phase B will replace this chart with a dual-instance StatefulSet
# co-located with workers on `app` nodes.
resources: {}

nodeSelector: {}
tolerations: []
affinity: {}

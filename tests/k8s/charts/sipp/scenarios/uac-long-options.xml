<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Long-call UAC for endurance: INVITE → 200 → ACK → 20-min hold → BYE.

  The B2BUA's keepalive rule (TimerRules.keepaliveRule) fires in-dialog
  OPTIONS to the UAC at intervals of `keepaliveIntervalSec` (default
  900s). For a 20-min call this means ~1 OPTIONS arrival per call.

  We intentionally DO NOT add an explicit `<recv method="OPTIONS"
  optional="true">` block here: sipp rejects an `optional=true` recv
  followed by `<pause>` ("`<recv>` before `<pause>` sequence without a
  mandatory message"). Instead we rely on sipp's default `pingreply`
  behavior which auto-replies 200 OK to any unmatched in-dialog
  request — exactly what the B2BUA's keepalive needs.

  Hold is hardcoded to 1200000ms (20 min). Sipp's `[$variable]` macro
  does NOT expand inside `<pause milliseconds="...">` attributes, only
  in `<send>` CDATA bodies — so we keep the literal here.

  RFC 3261 conformance:
    §11      In-dialog OPTIONS may be sent by either party; UAS responds
             200 OK echoing dialog tags + CSeq.
    §12.2.1.1 In-dialog request CSeq increments within the dialog; sipp's
             auto-200 echoes the CSeq verbatim from the request.
-->
<scenario name="K8s UAC long-options — 20-min hold; B2BUA OPTIONS auto-handled by sipp pingreply">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: K8s endurance long-call (OPTIONS keepalive)
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="200" rtd="true" rrs="true"/>

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      [routes]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <pause milliseconds="1200000"/>

  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      [routes]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="60000, 300000, 600000, 900000, 1200000"/>
</scenario>

apiVersion: v1
kind: Service
metadata:
  name: sipp-uas
  labels:
    app.kubernetes.io/name: sipp-uas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sipp-uas
  ports:
    - name: sip-udp
      port: 5060
      targetPort: sip-udp
      protocol: UDP
